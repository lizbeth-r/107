1)we need to install the lib: virtualenv
    python3 -m pip install virtualenv

2)we want to create the virtual enviroment

    python3 -m venv venv

3)active the virtual enviroment

    source venv/bin/activate

To install Flask

    python3 -m pip install Flask
    